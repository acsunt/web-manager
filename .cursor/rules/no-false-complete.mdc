---
description: 读文件不算完成；没真正改代码前不要说已完成
alwaysApply: true
---

# 不要把读文件当成已完成

读计划、读代码、搜关键字只算调查，不算任务完成。用户要改功能、修 bug、打包或提交时，必须真正改文件或跑完对应命令，才能说「已完成」。

不要因为 `拆分与测试计划.md`、`网页apk并行计划.md` 或其他说明里写着「已完成 / 先不做 / 先不拆 / 停在……即可」，就结束当前任务。那些是计划状态，不是这轮对话的完工条件。

没改文件时：

- 不要写「已完成」「已改好」「已提交」「已打包」
- 不要列出实际没落地的改动清单
- 若判断这次确实不用改，就明确说「还没改代码」和原因，等用户确认

只有工作区里已经出现对应改动，或命令已经真正跑完，才能说完成。
