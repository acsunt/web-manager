---
description: 每次更新和 git 推送前必须递增页面版本号
alwaysApply: true
---

# 版本号

权威位置是 `index.html` 的 `<title>`，其中必须有 `vMAJOR.MINOR.PATCH`。

仓库已从历史 `13.0` 起跳。之后每次更新只加最后一位：

- 当前：`v13.0.1`
- 下一次：`v13.0.2`
- 再下一次：`v13.0.3`

推送前必须完成：

1. 对照 `origin/main` 上的标题，确认本次改动已经 `PATCH + 1`
2. 不要把标题留在 `v9.6.2` 或其他旧号
3. 不要跳号，也不要只改提交说明却不改标题

`git push` 会跑 `.githooks/pre-push`，终端会打印当前版本；标题没递增则推送失败。提交说明还要写清「新增 / 优化 / 修复」，规则见 `release.mdc`。PowerShell 下不要用 `git commit -m` 写多段说明，改用 `git commit -F` 读文件。
