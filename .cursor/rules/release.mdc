---
description: 每次提交和推送都要写清新增、优化、修复，Release 会同时发布 HTML 和 APK
alwaysApply: true
---

# Release 说明和产物

推到 `main` 后，GitHub Release 会同时上传：

- `wan-vX.Y.Z-yes.html`
- `wan-vX.Y.Z-no.html`
- `web-manager-vX.Y.Z.apk`

Release 正文只来自**提交说明**，不来自聊天记录。

## 提交必须带这三段（没有的可省略，但不能一段都没有）

```
feat: 简短标题

新增
- 做了什么

优化
- 改了什么体验或性能

修复
- 修了什么问题
```

也可以写成 `新增 一句话`、`优化 一句话`、`修复 一句话`。

## 推送前

1. `index.html` 标题 PATCH +1（见版本号规则）
2. 提交正文里写清新增 / 优化 / 修复
3. `.githooks/pre-push` 会检查这两项；缺说明则推送失败
