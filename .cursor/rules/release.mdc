---
description: HTML 和 APK 都由本机 npm run apk 打进 dist 后上传同一 Release，并清掉旧产物
alwaysApply: true
---

# Release 说明和产物

分工：

- **本机 `npm run apk`**：先打单文件 HTML，再打 APK，然后把四份都上传到同一 Release `vX.Y.Z`
  - `dist/wan-vX.Y.Z-yes.html`
  - `dist/wan-vX.Y.Z-no.html`
  - `dist/web-manager-vX.Y.Z-32.apk`（32 位 armeabi-v7a）
  - `dist/web-manager-vX.Y.Z-64.apk`（64 位 arm64-v8a）
- **GitHub Actions**：不要再打包 HTML，也不要另写一套 Release 说明

本地打包默认输出到仓库根目录的 `dist/`，不要改到别的目录：

- `npm run build` → `dist/wan-vX.Y.Z-yes.html`、`dist/wan-vX.Y.Z-no.html`
- `npm run apk` → 先打上述 HTML，再打 `dist/web-manager-vX.Y.Z-32.apk`、`dist/web-manager-vX.Y.Z-64.apk`

离线 HTML 用的 Font Awesome / Cropper / Sortable / JSZip 放在 `vendor/`。打包直接读本地，不要每次从 CDN 下载。缺文件时先 `npm run vendor`。

`dist/` 只保留当前版本的 HTML 和 APK；打包时自动删掉旧的 `wan-v*` / `web-manager-v*`。

Release 正文只来自**提交说明**，不来自聊天记录。上传后不要另写一套说明。

本机已有完整 `gradle-8.14.3-all`。`android/gradle/wrapper/gradle-wrapper.properties` 必须用 `gradle-8.14.3-all.zip`，不要改成 `-bin.zip`，否则会再下一份发行包。

## 提交必须带这三段（没有的可省略，但不能一段都没有）

```
feat: 简短标题

新增
- 做了什么

优化
- 改了什么体验或性能

修复
- 修了什么问题
```

也可以写成 `新增 一句话`、`优化 一句话`、`修复 一句话`。

## 推送前

1. `index.html` 标题 PATCH +1（见版本号规则）
2. 提交正文里写清新增 / 优化 / 修复。PowerShell 不支持 `git commit -m "feat: ...` 这种多段提交写法，`-m` 传多行会被拆开或丢掉「新增 / 优化 / 修复」。先把完整说明写成 **UTF-8 无 BOM** 文件，再 `git commit -F 文件路径`。`脚本/git-commit-push.ps1` 同样用 `-F`，不要用 `-m` 拼多段正文，也不要用 `Set-Content -Encoding utf8`（Windows PowerShell 5 会写成带 BOM，Git 可能把中文读乱）
3. `.githooks/pre-push` 会检查这两项；缺说明则推送失败

## 推送后

1. 本机运行 `npm run apk`：打 HTML、打 APK、清掉 `dist` 旧文件、把 HTML 和 APK 一起上传到该 Release
