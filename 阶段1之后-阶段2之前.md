# 阶段 1 之后、阶段 2 之前：可靠性补丁

本文不拆业务模块，也不改存储。只修两件会把页面弄坏的事：

1. 用显式 handler 对象替换 `eval(name)`
2. 渲染名称、路径、URL 时走 `textContent` 或转义（搜索和树选择优先）

和 `拆分与测试计划.md` 的关系：阶段 0、1 已完成；阶段 2 是主页数据结构迁移。这两步插在中间，**可与拆分并行思考，但不要并进同一次提交**。做完再进入阶段 2。

判断标准仍是：不打开页面、只给一份数据，能不能断言结果？  
handler 表和转义函数能。20 多个弹窗的 `onclick` → `addEventListener` 不能，本文件不做。

## 1. 为什么现在做

模块化之后，函数在模块作用域里，HTML 的 `onclick="toggleEditMode()"` 找不到它们。现在的补法是：扫描内联事件名，再 `eval(name)` 挂到 `window`。`index.html` 大约有 117 处内联事件。

这主要脆，不主要是攻击面——函数名来自自己的 HTML，不是用户输入。缺函数时只会 `console.warn`，按钮可能静默失效。

更危险的是用户字符串走 `innerHTML`：

- 树节点名称：`nameSpan.innerHTML = node.name.replace(/\n/g,'<br>') + ...`
- 搜索结果：名称、路径、URL、备注、`wsId` 拼进 HTML，并带 `onclick="jumpToNode(${m.node.id}, '${m.wsId}')"`
- 树选择 / 分类下拉：`${node.name}`、`${m.name}` 直接插入

导入一份名称里带 `<img onerror=...>` 或 `</div><script>` 的备份，搜索和树会按 HTML 执行。这和拆 `workspace.js` 无关，越晚修，后续渲染代码越多。

## 2. 不做什么

| 不做 | 原因 |
|---|---|
| 把 117 处 `onclick` 全改成 `addEventListener` | 漏绑一个按钮页面就坏；冒烟覆盖不住 20 多个弹窗 |
| 清掉所有 `innerHTML` | 图标、置顶图钉、统计数字是固定 HTML，转义会把图标变成字面量 |
| 自定义代码默认只允许 CSS | 那是主题里 `innerHTML` + 重放 `script` 的另一条路径，单独处理 |
| 修删除弹窗破损 HTML | 仍按主计划，不要和这两步混 |
| 改 IndexedDB、抽 `workspace.js` | 阶段 8 / 阶段 2 的事 |

HTML 内联事件可以以后再改。本文件只要求：运行时不再按字符串 `eval` 找函数。

## 3. 两小步，两次提交

### 步骤 A：显式 handler 表替换 `eval`

**改动面：** `main.js` 末尾注册逻辑；必要时给 `ui.js` 加一个「按表注册」的薄封装。不要改 `index.html` 的 `onclick`。

现在：

```js
const inlineHandlerNames = collectInlineHandlerNames();
// ...再补几个扫描不到的名字
inlineHandlerNames.forEach((name) => {
    const handler = eval(name);
    if (typeof handler === 'function') inlineHandlers[name] = handler;
});
registerWindowHandlers(inlineHandlers);
```

改成：

```js
const inlineHandlers = {
    toggleEditMode,
    openAddModal,
    saveData,
    jumpToNode,
    // ...HTML 实际用到的函数，写全
};
registerWindowHandlers(inlineHandlers);
```

`collectInlineHandlerNames()` 可以留下来做校验：表里缺了 HTML 用到的名字，启动时 `console.warn`，不要再 `eval`。动态拼出来的 `onclick`（搜索结果、工具栏配置项、清空主页列表）也必须出现在表里。

**测试：** `tests/ui.test.js` 已能测「从 HTML 抽出 onclick 名」。补一条：给定一段含 `onclick` 的 HTML，handler 表覆盖这些名字，且注册过程不调用 `eval`。

**完成标准：** 页面按钮仍可用；源码里不再有 `eval(name)`；缺函数时启动就能看出来。

**注意事项：**

- 表必须写全。漏了 `jumpToNode`，搜索能出结果但点不进去。
- 不要为了少写几行又去扫 DOM 再 `eval`。宁可表长，不可再猜作用域。
- `registerWindowHandlers` 仍需要：静态 HTML 的内联事件还在。
- 动态 `innerHTML` 里的 `onclick` 依赖挂到 `window` 的函数；步骤 A 之后它们必须在表里。步骤 B 会消掉搜索那一处动态 `onclick`，但不要假设步骤 A 完成时它已经消失。

### 步骤 B：用户字符串转义，搜索和树选择优先

**改动面：** 渲染，不是搜索匹配。阶段 4 抽的是 `searchWorkspaces` 的纯函数；本步改的是 `handleSearchInput` 的 HTML 拼接，以及 `createNodeEl` / 树选择。

**优先改这些：**

1. 搜索结果：`m.node.name`、`m.path`、`m.wsName`、`mainUrl`、`m.node.note`、`m.subMatch`
2. 树节点名称：分类 `nameSpan`、网页 `page-name`
3. 树选择：`openTreeSelectModal` 里的 `${node.name}`
4. 分类下拉：`handleCategorySearch` 里的 `${m.name}`

建议抽一个纯函数，例如 `escapeHtml` 放 `utils.js`，或渲染时改用 `textContent` / `createTextNode`。固定图标 HTML 继续 `innerHTML`，但用户文本不要拼进去。

搜索结果不要再拼 `onclick="jumpToNode(...)"`。改成创建 DOM 节点后 `addEventListener`，`wsId` 用 `dataset` 或闭包，避免引号截断。

分类名现在用 `replace(/\n/g,'<br>')` 显示换行。改 `textContent` 后换行会变成一行。保留换行用 `white-space: pre-line`，或按 `\n` 拆成文本节点 + `<br>`，**不要**把原始名称当 HTML 插入。

**测试：** `tests/utils.test.js` 补 `escapeHtml`（若抽出）：`<`、`>`、`&`、`"`、`'`。不要给整页 `createNodeEl` 写 jsdom 大测试。

**完成标准：** 名称为 `<img src=x onerror=alert(1)>` 的分类，树上和搜索里显示字面量，不弹窗、不丢节点。搜索仍能跳转。

**注意事项：**

- 只转义用户字符串。图钉、`cat-count`、类型徽章、Font Awesome 图标保持原样。
- `getIconHtml` 的自定义图标是 `src="${node.customIcon}"`，本步可以不扩到图标 URL；那是图，不是名称。不要顺手改主题自定义代码。
- `showToast` 目前也走 `innerHTML`。若 toast 会带用户名，一并转义；纯固定文案可暂留。
- `api.js` 用 `<textarea>.innerHTML` 解码标题实体，那是解码不是渲染，不要删。
- 步骤 B 会动 `createNodeEl`。阶段 1 的树单测不绑 DOM，不受影响；页面冒烟要再跑一遍：打开、新增、搜索跳转、置顶、删除、刷新。

## 4. 和主计划的边界

| 事项 | 放哪 |
|---|---|
| 步骤 A / B | 本文件，阶段 1 后、阶段 2 前，两次提交 |
| 抽 `workspace.js` | `拆分与测试计划.md` 阶段 2 |
| 抽搜索匹配 | 阶段 4，不抽 HTML |
| IndexedDB | 阶段 8 |
| 全站 `addEventListener`、自定义代码禁 JS、删除弹窗 | 仍不在拆分主线，也不在本文件 |

不要和阶段 2 混。失败时要能分清是 handler 漏了、转义坏了，还是主页迁移坏了。

## 5. 验收

每一步做完：

1. `npm test` 全绿
2. `npm run test:e2e` 全绿
3. 打开 `index.html`：工具栏按钮可点；新增分类 / 网页；搜索能跳转
4. 步骤 B 额外：建一个名称含 `<` 和换行的分类，树上显示字面量且换行还在，搜索点它能跳转
5. 控制台没有新增报错，也不再出现 `eval`

任一步验收失败，先回退该步，不要开始阶段 2。
