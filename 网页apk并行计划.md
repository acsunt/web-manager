# 网页 / APK 并行计划

本文只覆盖一件事：网页继续当主入口，Android APK 只增强「识别标题和图标」。不重写整站，也不把网页识别改成只能在 App 里用。

打包工具你已经有，而且打过别的软件。本仓库目前没有 Android 工程，第一次是把现有页面塞进壳，并接一条原生识别通道。

## 1. 要达成的目的

1. **网页和 APK 并行，不二选一。** 浏览器照常打开 `index.html` / 单文件 HTML；手机再多一个 APK。
2. **APK 识别走原生通道。** 隐藏 WebView 真打开目标网址，再取页面标题和图标。成功率高于现在的代理抓 HTML。
3. **网页识别维持现状。** 浏览器里没有原生桥，继续走字典 + `api.allorigins.win` + Google favicon / iowen。
4. **业务代码只维护一份。** 列表、编辑、主题、导入导出、存储逻辑共用。不要分出「网页版源码」和「App 版源码」。
5. **两条识别路互不串。** 有原生桥才走 App；没有就走网页。网页失败不影响 APK，APK 失败也不改网页默认逻辑。

## 2. 当前代码在做什么

识别不是「打开网页」，而是浏览器里打第三方接口。

| 能力 | 现在怎么做 | 入口 |
|---|---|---|
| 单条标题 | `fetchPageTitleFor`：常见站字典 → allorigins 抠 `<title>` → 域名首段兜底 | 编辑弹窗「识别」`fetchPageInfo` |
| 批量名称 | 同上 | `applyBatchIconName`（来源选「识别」时） |
| 批量图标 | Google `s2/favicons`，失败改 iowen | `checkAllIcons` |
| 图标地址生成 | `api.js` 的 `createFaviconUrl` | 预览 / 自动图标 |

关键限制：

- 网页有 CORS，不能直接 `fetch` 任意站点，所以才走 allorigins。
- 很多站是 JS 渲染，代理拿到的 HTML 里没有最终标题。
- Google / iowen 给的常常不是站点自己的高清图标。
- 把现有页面原样打成 APK，**不会自动变成模拟打开网页**。壳不写原生桥，识别还是打那几个接口。

## 3. 并行怎么互不干扰

```
识别标题 / 图标
  ├─ APK：检测到原生桥 → 隐藏 WebView 打开网址 → 回传 title / icon
  └─ 网页：没有原生桥 → 继续字典 + allorigins + Google / iowen
```

JS 侧只在识别函数里分叉，例如：

```js
async function fetchPageTitleFor(url) {
  if (window.Android?.fetchPageInfo) {
    return window.Android.fetchPageInfo(url);
  }
  // 现有网页逻辑：字典 + allorigins + 域名兜底
}
```

图标同样：APK 有 `fetchPageIcon` 就用原生结果；没有就走现在的 Google / iowen。

| | 网页 | APK |
|---|---|---|
| `window.Android`（或打包工具注入的同类桥） | 没有 | 有 |
| 识别 | 代理抓 HTML | 模拟打开网页 |
| 列表 / 主题 / 备份 | 同一份前端 | 同一份前端 |
| 数据 | 浏览器存储 | App WebView 自己的存储 |

「互不干扰」指运行时两条路不串、数据不自动混。源码仍是一份，所以改列表、主题、导入导出时，网页和 APK 会一起变。这是并行的本意，不是互相踩脚。

## 4. 注意事项

### 4.1 不要做的事

- 不要为了 APK 重写整站，或拆出第二套 `main.js`。
- 不要把原生识别写成网页的默认路径。浏览器里没有 WebView 桥，硬走会直接失败。
- 不要指望「只打包、不写桥」就能提高识别率。
- 不要让 APK 和网页抢同一份云同步。当前没有账号体系，两份数据靠 JSON / ZIP 导入导出。
- 不要在网页识别失败时静默改成「假装成功」。APK 桥失败应回退网页方案或明确报失败，不要写坏图标。
- 不要把 `file://` 和盘符路径丢给原生 WebView 去打开公网识别。现有 `checkAllIcons` 已经跳过本地路径，APK 也要跳过。

### 4.2 数据

- 电脑 Chrome、手机 Chrome、APK WebView 是三个源，存储互不相通。这和现在 `file://` 与本地服务器不是同一个源是同一类问题。
- 迁移书签继续用现有备份 / 恢复，不要另做一套 App 专用格式。
- 主题图、`customIcon` 的 `data:image` 可能很大。APK WebView 存储上限和浏览器不同，大图仍优先用图床 URL。

### 4.3 原生识别本身

- 模拟打开网页更准，但更慢。批量识别不要照搬现在的 5 路并发去开 5 个 WebView；APK 建议 1–2 路，避免内存打满。
- 要等页面加载，设超时（建议 8–15 秒）。超时后记失败，不要一直挂着。
- 有的站会跳转、拦 WebView、要登录。标题可能变成登录页标题，图标也可能是站点通用图标。成功率高于网页，不是 100%。
- 优先用 WebView 回调的 title / favicon（Android 上常见是 `onReceivedTitle` / `onReceivedIcon`）。不要再在原生里打 allorigins。
- 图标尽量转成可存的 `data:image` 或稳定 URL，再交给现有的 `iconType = 'custom'` / `customIcon`。
- 标题写回规则保持和现在一致：批量图标识别里，只有识别结果长度 `<= 12` 才改 `page.name`；单条识别仍写入「识别到的名称」，不要擅自改用户已填名称。

### 4.4 打包和前端约束

- 继续保持静态页面可双击打开。APK 只是多一个发行壳，不要把项目改成必须 npm 才能运行的网页。
- APK 入口已固定：先 `npm run build` 生成离线单文件 `dist/wan-vX.Y.Z-no.html`，再复制进 `android/app/src/main/assets/index.html`。包名 `com.webmanager.app`，需要联网权限。日常改前端后执行 `npm run apk`。
- APK 的 `versionName` 由 Gradle 直接读取 `index.html` 标题里的 `vMAJOR.MINOR.PATCH`，例如网页是 `v13.0.11`，APK 也是 `13.0.11`。Android Studio 单独打包同样跟网页号，不另维护一份。
- 页面还引用 Font Awesome、Cropper、Sortable、JSZip 的 CDN。APK 用离线单文件，把这些资源打进 HTML。
- 识别桥的名字是 `window.Android.fetchPageInfo(url, requestId)`，网页代码必须先探测再调用。
- 本计划不改版本号规则。真正改代码并推送时，仍按 `index.html` 标题 `vMAJOR.MINOR.PATCH` 最后一位 +1。

### 4.5 和现有拆分计划的关系

- 拆 `main.js`、迁 IndexedDB 仍按 `拆分与测试计划.md`。本文件不插入那些阶段。
- 识别分叉应集中在现有入口：`fetchPageTitleFor`、`fetchPageInfo`、`checkAllIcons`。不要新开一套识别 UI。
- 网页方案的单元测试继续测 `api.js` 的代理解析。原生桥测不了就不要为了测试去 mock 一整页 WebView。

## 5. 步骤

你已有打包工具，下面按「先能装上，再换识别通道」做。不要第一步就改识别逻辑。

### 步骤 0：约定入口

1. 确认打包工具吃的是 `index.html` 目录，还是 `dist` 里的单文件 HTML。
2. 若走单文件：先 `npm run build`，再把生成的 HTML 丢进打包工具。
3. 记下包名、是否要联网权限。识别公网网址必须有网络权限。

完成标志：能说清「以后改前端，APK 打哪份文件」。

### 步骤 1：原样打一版，验证页面能跑

1. 用当前网页打一个调试 APK，不接识别桥。
2. 真机打开：列表、新增网页、主题、导入导出能用。
3. 这时点「识别」，行为应和网页一样（仍走 allorigins / Google）。这是基线，不是最终效果。

完成标志：APK 能当网页用，数据能存、能备份。识别准不准先不管。

### 步骤 2：前端加探测，行为先不变

1. 在 `fetchPageTitleFor`（以及图标识别）最前面加：有原生桥才调用，没有就走旧逻辑。
2. 网页浏览器打开，确认识别和现在完全一样。
3. APK 里如果桥还没接上，也应自动走旧逻辑，不能报 JS 错。

完成标志：没桥时网页 / APK 都不崩；有桥时以后只改这一处。

### 步骤 3：原生隐藏 WebView 通道

在打包工具 / Android 壳里加（名称按工具调整）：

1. 隐藏 WebView，加载目标 `http(s)` URL。
2. 收到标题、图标后回传给 JS。建议一次回传 `{ title, icon }`，避免标题和图标各走一套时序。
3. 加载失败、超时、非 http(s) 明确失败。
4. 用完销毁或重置 WebView，避免批量识别泄漏。

完成标志：JS 能 `await` 到一条真实页面的标题；图标有则带回，没有则空字符串。

### 步骤 4：把三个识别入口接到桥上

| 入口 | APK 应改成 |
|---|---|
| `fetchPageInfo` | 优先用原生 `{ title, icon }`；标题填「识别到的名称」；图标仅在用户选自动/需要时写入预览 |
| `applyBatchIconName`（识别来源） | 只要原生 title |
| `checkAllIcons` | 原生 icon 优先写入 `customIcon`；title 仍按现有 `<= 12` 规则决定是否改名 |

网页这三个入口保持旧实现。不要复制三份原生调用，都走步骤 2 的同一处分叉。

完成标志：APK 里识别一个百度 / GitHub / 普通博客，标题和图标明显比网页版稳；同一操作在电脑网页上结果仍和改之前一致。

### 步骤 5：批量识别限流

1. APK 批量改为 1–2 并发，或原生队列一个接一个打开。
2. 保留现有暂停 / 中止（`isCheckPaused` / `isCheckAborted`），原生加载也要能取消。
3. 报告弹窗继续用现有「已识别 / 失败」列表，不要另做 App 进度页。

完成标志：几十条网址连认时 App 不闪退，暂停和中止有效。

### 步骤 6：对照验收

在网页和 APK 上各测一遍：

1. 常见站（字典能命中的）：两边都应有中文名；APK 图标应更接近站点真实图标。
2. JS 渲染站：网页可能只有域名兜底，APK 应能拿到加载后的 title。
3. 本地文件路径：两边都不识别。
4. 无网：网页失败；APK 也失败，并给出失败而不是写上错误图标。
5. 导入同一份 JSON：两边都能恢复列表。数据仍是两份，不要求自动同步。

全部通过后，APK 才算「增强版识别入口」。此前那版原样打包只算壳可用。

## 6. 时间（已有打包工具）

第一次贵在接桥，不在「会不会打包」。

| 阶段 | 大概耗时 | 做什么 |
|---|---|---|
| 步骤 0–1 | 0.5–2 小时 | 选入口、打出能用的调试 APK |
| 步骤 2 | 不到 1 小时 | JS 探测，网页行为不变 |
| 步骤 3–4 | 2–6 小时 | 隐藏 WebView + 接到三个识别入口 |
| 步骤 5–6 | 1–3 小时 | 限流、真机对照 |

后面日常：

| 改了什么 | 大概多久 |
|---|---|
| 只改网页功能 | 重新打进壳，约 1–5 分钟 |
| 改识别桥 / 权限 / WebView | 10–30 分钟编译 + 真机看识别 |
| 环境、签名、包名已固定 | 不必再走第一天的搭壳 |

## 7. 完成定义

同时满足才算并行做成：

- 网页双击 / 本地打开，识别逻辑与现在一致。
- APK 识别走隐藏 WebView，不再依赖 allorigins 才能拿到标题。
- 没有原生桥时，代码自动回退网页方案，不抛错。
- 书签数据仍靠现有备份格式搬家，没有 App 专用结构。
- 没有把项目改成离开打包工具就打不开的网页。
