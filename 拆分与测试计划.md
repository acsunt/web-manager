# 拆分与测试计划

本文覆盖「怎么拆 `main.js`」「怎么加测试」，以及书签 / 图片从 `localStorage` 迁到 IndexedDB。阶段 1 之后、阶段 2 之前的 handler 表和名称转义见 `阶段1之后-阶段2之前.md`。删除弹窗、自定义代码等见 `项目建议.html`。

## 1. 结论

不要先整文件大拆，也不要先对着 `main.js` 写测试。

正确顺序是：

1. 先给已经独立的 `utils.js` 写测试。
2. 再按业务域从 `main.js` 往外抽。
3. **每抽出一块，立刻给那一块写测试。**

判断标准：不打开页面、只给一份 JSON，能不能断言结果？

- 能：先抽成模块，马上写测试。
- 不能：先留在 `main.js`，不要为了测试去硬拆 UI。

拆分的目的不是把 `main.js` 变小。纯逻辑只占一小块，计划全部做完后它仍会有大约 80–110KB。真正要的是：**改搜索进 `search.js`，改导入进 `io.js`，修完能马上跑测试。** 为了缩小文件去拆主题、拖拽、弹窗，反而会让加功能和修 bug 更麻烦。

也不要再发明新文件。漏掉的规则并进已有模块即可，见第 5.1 节和第 5.5 节。

## 2. 当前状态

| 文件 / 阶段 | 现状 | 对测试的影响 |
|---|---|---|
| `utils.js` | 已是纯函数，有测试 | 完成 |
| `api.js` | 网络封装，有测试 | 完成 |
| `ui.js` | 轻量 DOM 工具，有测试 | 完成 |
| `tree.js` | 查找 / 删除 / 置顶 / 去重 id 已抽出 | 完成；插入和「未分类」还在 `main.js`，见阶段 1 补遗 |
| `workspace.js` | 主页结构迁移、删主页兜底已抽出 | 完成；列表 UI 故意留在 `main.js` |
| handler 表 / 名称转义 | `inlineHandlers` 已接上 | 完成 |
| `main.js` | 约 199KB；导入、搜索、文本、重复、树插入仍堆在一起 | 直接测会绑死全局状态 |
| `index.html` | 入口 + 20 多个弹窗 + 内联事件 | 不适合当第一批测试对象 |
| 测试基础设施 | Vitest + 少量 Playwright | 底座已有，不必再搭 |

`tree.js` 已抽出后，`main.js` 里的薄封装仍可默认读全局 `data`，测试只认模块签名。后面再拆主题、导入导出，都碰不到这组测试。

## 3. 原则

### 3.1 拆和测绑在同一步

一次只搬一块。完成标志不是「文件变小了」，而是「新模块有测试，旧页面行为没变」。

### 3.2 抽出的是纯逻辑，不是整页 UI

适合先拆、也适合先测的函数：

- 输入是数据，输出是数据
- 不碰按钮、弹窗、拖拽、裁图

`main.js` 只做薄封装，例如：

```js
import { findNode as findNodeInTree } from './tree.js';

function findNode(id, list = data) {
    return findNodeInTree(id, list);
}
```

这样旧页面暂时不用改交互，测试也不用 import `main.js`。

### 3.3 测试绑模块，不绑入口

测试文件只 import 新模块，例如 `tree.js`、`search.js`、`io.js`。  
不要从 `main.js` 测业务规则。`main.js` 应逐渐变成编排层。

### 3.4 保持静态页面可双击打开

继续用 ES module。不要为了测试把项目改成完整前端工程，也不要上 React。  
Vitest 只服务测试，不参与页面运行。

### 3.5 先别做的事

- 一次性把 `main.js` 拆完再补测试
- 给整份 `main.js` 写 jsdom 大测试
- 视觉回归 / 截图对比
- 给 `style.css` 写测试
- 给 20 多个弹窗逐个点
- 一上来就上 Cypress 或很重的 E2E
- 测长按菜单、分类拖拽、Cropper、主题滑条、`checkLinks`
- 为了把 `main.js` 拆到几十 KB 而搬走 UI
- 新增 `save.js` / `export.js` / `title.js` 这类文件

`checkLinks` 用的是 `no-cors` HEAD，结果不稳定，不适合当回归基线。

## 4. 目标结构

```text
web-manager-main/
  index.html
  style.css
  main.js                 # 编排：状态、事件、render 调用
  utils.js                # 已有：URL / 计数 / sanitize
  api.js                  # 已有：fetch / 标题 / favicon
  ui.js                   # 已有：toast / 下载 / 内联事件收集
  tree.js                 # 查找、删除、插入、未分类、置顶、去重 id
  workspace.js            # 主页 CRUD、分组、旧数据迁移
  search.js               # 搜索匹配，返回结果数组
  text-io.js              # 文本导出、粘贴解析
  io.js                   # JSON/ZIP 导入分流 + 导出命名
  duplicates.js           # 全站重复网址检测
  storage.js              # IndexedDB；导入锁住前不要做
  theme.js                # 先不拆，见第 10 节
  toolbar.js              # 先不拆
  icon.js                 # 先不拆
  tools.js                # 先不拆，网络检测保持手动
  tests/
    utils.test.js
    tree.test.js
    workspace.test.js
    search.test.js
    text-io.test.js
    io.test.js
    duplicates.test.js
    storage.test.js
    api.test.js
    ui.test.js
  package.json
  vitest.config.js
```

前期不必一次建齐。每完成一个阶段，只新增对应模块和测试文件。  
**不要扩这份名单。** 漏写的规则并进 `tree.js` / `io.js` / `api.js`，不要新开文件。

## 5. 阶段计划

每阶段都按同一循环执行：

1. 把纯逻辑从 `main.js` 剪到新文件并 `export`
2. `main.js` 改为 import + 薄封装
3. 给新模块写测试
4. 本地跑通 `npm test`
5. 手动确认页面：打开、新增、刷新后数据还在

执行优先级（编号保留，方便对照旧文档）：

| 优先级 | 阶段 | 原因 |
|---|---|---|
| 已完成 | 0、1、1 之后 handler、2 | 底座和数据骨架已在 |
| **加紧** | 5 `io.js` | 分错会覆盖书签；导出命名是导入的反操作，必须成对锁 |
| 继续 | 1 补遗（树插入）、3 文本、6 重复 | 改默认位置、粘贴格式、保存误报时会反复碰到 |
| 有空 | 4 搜索 | 搜错了最多找不到，不会丢数据 |
| 先不拆 | 7 扩冒烟、8 IndexedDB、9 主题/工具栏/图标/死链 | 见第 10 节 |

### 阶段 0：补测试底座，先测现成模块

**状态：** 已完成。  
**拆文件：** 不拆。

**要做：**

- 增加 `package.json`、`vitest.config.js`
- 使用 Vitest + jsdom
- 脚本：`"test": "vitest run"`、`"test:watch": "vitest"`
- `.gitignore` 补上 `node_modules/`

**测试文件：** `tests/utils.test.js`

| 函数 | 必测用例 |
|---|---|
| `normalizeUrls` | 只有 `url`；只有 `urls`；两者都空；空节点 / `undefined` |
| `countPages` | 单页；嵌套分类；分类无 `children` |
| `countTotalPages` | 多根节点；非数组返回 0 |
| `sanitizeData` | 分类缺 `children` 时补 `[]`；深层嵌套；传入非数组返回 `[]` |
| `isLocalUrl` | `file://`、`C:\`、`D:/`、普通 `http` |
| `normalizeWebUrl` | 补 `https://`；已有协议不改；本地路径不乱补；空白返回空 |

可选补测：

- `api.js`：`createFaviconUrl`；`request` 超时 abort；`fetchPageTitle` 解析 `<title>` 和 HTML 实体
- `ui.js`：`collectInlineHandlerNames` 能从一段 HTML 抽出 `onclick` 函数名

**完成标准：** `npm test` 能跑；页面双击打开方式不变。

### 阶段 1：树操作 `tree.js`

**状态：** 主体已完成；插入和「未分类」还没抽，见补遗。

这是数据正确性的底座，所有移动 / 删除 / 搜索都依赖它。

**从 `main.js` 抽出：**

- `findNode`
- `findParent`
- `deleteNode`
- `sortNodesByPin`
- `cleanDuplicates`（去重缺失 / 重复 id，不要在模块里直接 `save()`）
- `collectSelfAndDescendantIds`
- `getAllPages`

签名改成显式传入树，不再默认读全局 `data`：

```js
export function findNode(id, list) { ... }
export function deleteNode(id, list) { ... }
export function cleanDuplicateIds(nodes) { ... }
```

`main.js` 继续负责调用 `save()` / `renderTree()`。

**测试文件：** `tests/tree.test.js`

必测：

- 根节点、深层子节点、找不到返回 `null`
- `findParent`：根为 `null`，子节点返回父分类，找不到返回 `undefined`
- `deleteNode`：删除叶子、删除分类、删除后兄弟还在
- `sortNodesByPin`：置顶节点始终在前，递归处理子树
- `cleanDuplicateIds`：缺 id、重复 id 会生成新 id，且同树内唯一
- `collectSelfAndDescendantIds`：包含自己和全部后代
- `getAllPages`：能带出路径，分类本身不进结果

**完成标准：** 树测试全绿；页面里删除、置顶、刷新后结构仍正确。

#### 阶段 1 补遗：插入和「未分类」

**状态：** 未做。并进现有 `tree.js`，不要新建 `save.js`。  
**原因：** `saveData()` 里的默认落点和 before / after / inside 是树规则，不是表单 UI；以后改「网页默认进哪」会反复碰到。

从 `saveData()` 只抽出纯数据部分：

```js
export function ensureUncategorized(tree) { ... }
export function insertNode(tree, node, { parentId, relPos, addToTop }) { ... }
export function moveNode(tree, id, { parentId, pos }) { ... }
```

不要把整个 `saveData()` 搬走。读表单、重复确认弹窗、`alert` 仍留在 `main.js`。

必测：

- 网页没有父级时，自动进入名为「未分类」的分类；没有就新建
- `inside` / `before` / `after` 插入位置正确
- 编辑时换父级，原位置消失、新位置出现
- 分类本身不误进「未分类」

建议在阶段 5 之后、阶段 3 前后做。导入锁住后再动保存路径，出问题比较好分。

### 阶段 2：主页与旧数据迁移 `workspace.js`

**状态：** 已完成。

**从 `main.js` 抽出：**

- `createDefaultAppData`
- `updateDataPointer` 的纯数据部分
- `init()` 里的旧数据迁移：数组 → `{ workspaces, currentId }`
- 删除主页后的兜底：删光后重建默认主页；删当前主页后切到第一个
- 分组字段补齐：`workspaceGroups` 缺失时从 `workspaces[].group` 重建

不要把 `prompt` / `confirm` / `renderWorkspaceList` 一起搬走。UI 仍留在 `main.js`。  
不要在这一阶段改存储介质。阶段 2 只迁数据结构（旧数组 → `{ workspaces, currentId }`），IndexedDB 放到阶段 8，且现阶段先不做。

**测试文件：** `tests/workspace.test.js`

必测：

- 无存档时创建默认主页
- 旧格式 `[{ type, name, ... }]` 能迁成 `workspaces`
- 损坏 JSON / 非数组非对象时回退默认数据
- `currentId` 指向不存在的主页时，切到第一个
- 删除当前主页后 `currentId` 更新
- 删光全部主页后重建「主页」
- 缺 `workspaceGroups` 时按已有 `group` 补齐

**完成标准：** 用一份旧数组 JSON 跑迁移测试通过；手动刷新页面后主页还在。

### 阶段 3：文本导入导出 `text-io.js`

**状态：** 未做。排在阶段 5 之后。  
**原因：** 粘贴和导出预览会静默对不上，但不会覆盖整份书签；先锁导入更划算。

**从 `main.js` 抽出：**

- `generateTextExport`
- `generatePathExport`
- `confirmParsePaste` 里的解析部分（输入纯文本，输出 `{ name, note, urls }`）
- `resolveBatchNameTemplate`

解析不要在模块里写 `editName` / `urlListContainer`。`main.js` 只负责把解析结果填进表单。  
批量改名那套弹窗、进度列表仍留在 `main.js`；只把 `%d` 模板并进本模块，不要为此新建文件。

**测试文件：** `tests/text-io.test.js`

必测：

- 分类 + 网页 + 多网址 + 备注的树形文本
- 路径格式导出
- 名称 / 备注里的换行被转成 `\n`
- 解析能吃回 `📄 🔗 🗒️` 导出文本
- 支持 `🔗 URL (名称)` 和 `🔗 URL 名称`
- 无 emoji 时，纯 http 行也能进 `urls`
- `%d` 模板替换：`页面%d` + index `3` → `页面3`
- 空模板返回空字符串

**完成标准：** 导出文本再解析，名称、备注、网址能对上。

### 阶段 4：搜索 `search.js`

**状态：** 未做。排在文本和重复检测之后。  
**原因：** 匹配规则值得测，但搜错了不会覆盖书签；不是加紧项。

**从 `main.js` 抽出匹配逻辑，不要抽出 HTML 拼接。**

目标接口：

```js
export function searchWorkspaces(appData, query) {
    // 返回 [{ node, path, subMatch, wsName, wsId }, ...]
}
```

`handleSearchInput` 继续负责显示 / 隐藏结果和生成 HTML。

**测试文件：** `tests/search.test.js`

必测：

- 按名称匹配分类和网页
- 按备注匹配
- 按子网址 URL / 名称匹配，并带上 `subMatch`
- 跨主页搜索
- 大小写不敏感
- 空字符串 / 只有空格返回空数组
- 不把分类的 `children` 误当成网址

**完成标准：** 搜索测试不依赖 DOM；页面搜索跳转仍可用。

### 阶段 5：导入分流与导出命名 `io.js`

**状态：** 未做。**现在就做。**  
**原因：** 主题包、图标、完整备份、单树混在一个入口，分错会覆盖书签。ZIP 文件夹名和 `001_主页 (12).json` 是导入、导出共用的规则，必须成对抽、成对测。

**从 `main.js` 抽出纯判断和纯合并，不抽 `FileReader` / `JSZip` / `confirm`。**

建议接口：

```js
export function classifyImportPayload(json, fileName)
export function mergeWorkspaceBackup(appData, incoming, { overwrite })
export function mergeTreeIntoWorkspace(currentTree, incomingTree, { overwrite })
export function parseZipWorkspaceName(zipPath)
export function shouldAutoOverwriteEmptyDefault(appData)
export function buildZipExportLayout(workspaces, workspaceGroups)
```

`buildZipExportLayout` 来自 `exportJsonFile()` 的纯命名部分：`000_未分类`、分组文件夹、`001_主页 (12).json`。下载和 `JSZip` 仍留在 `main.js`。  
不要为此新建 `export.js`。导入解析和导出命名是同一套规则的正反面。

`importFromFile` 留在 `main.js`，只负责读文件、弹确认、调用上面这些函数。不要再拆 `io-ui.js`。

**测试文件：** `tests/io.test.js`

必测：

- 主题配置（有 `theme` + `day`）不能当书签树导入
- ZIP 内含 `theme_config.json` 应识别为主题包
- `type === 'web_manager_icon_settings'` 只更新图标，不改树结构
- 完整备份 `{ workspaces, workspaceGroups }` 覆盖 / 追加
- 单数组树导入当前主页：空树直接写入；有数据时覆盖或追加
- 空默认主页（名称是「主页」或「新主页」且 `data` 为空）自动覆盖，不需要确认
- 导入后重新生成 `ws.id`，避免和现有主页冲突
- ZIP 文件夹名：`000_未分类` 视为无分组；`001_工作` 抽成分组 `工作`
- 文件名 `001_主页 (12).json` 抽成名称 `主页`
- `sanitizeData` 会作用到导入的分类树
- 导出布局再交给 `parseZipWorkspaceName`，分组和主页名能对回来

**完成标准：** 用夹具 JSON 覆盖上述分流；手动导入一份备份，树不丢、主题文件不会写进主页数据。

### 阶段 6：重复检测 `duplicates.js`

**状态：** 未做。排在阶段 3 前后。  
**原因：** 保存网页时的误报 / 漏报很烦，但依赖 `getAllPages`，不依赖导入分流；锁住导入后再抽即可。

**从 `main.js` 抽出：**

- `checkDuplicates` 的纯计算部分
- `saveData()` 里保存前的重复预检

URL 规范化规则保持现状：去尾斜杠、转小写。

```js
export function normalizeDuplicateKey(url)
export function findDuplicateUrls(workspaces, { ignoreNodeId } = {})
```

**测试文件：** `tests/duplicates.test.js`

必测：

- `https://A.com/` 和 `https://a.com` 视为重复
- 同一网页内部两个相同 URL 要报
- 跨主页重复要报，并带上主页名和路径
- 编辑自己时，`ignoreNodeId` 不把当前节点当冲突
- 本地 `file://` 和盘符路径是否纳入，按当前产品行为锁死，不要顺手改规则

**完成标准：** 重复规则有测试；保存网页时的确认逻辑仍由 `main.js` 弹窗。

### 阶段 7：少量页面冒烟

**状态：** 已有 `tests/e2e/tree-persist.spec.js`。先不要扩成 5 条。  
**原因：** 这是测试，不是拆文件。导入单测还没有时，整页导入冒烟失败分不清是分流错了还是页面错了。

现有「新增后刷新还在」先够用。等阶段 5 有单测后，再补一条最值钱的即可：

- 导入主题文件时，不会把书签数据覆盖掉

完整 5 条清单（阶段 8 之前不必一次做齐）：

1. 打开页面，能看到默认主页
2. 新增分类和网页，刷新后还在（阶段 8 前可读 `localStorage`；迁完后只断言页面和内存数据，不要写死 `webManagerDataProMax`）
3. 搜索能跳到对应节点
4. 导出 JSON，再导入回来树结构一致
5. 导入主题文件时，不会把书签数据覆盖掉

不要把长按菜单、拖拽层级、图片裁切、死链检测写进冒烟。

**完成标准：** 失败时能看出是数据问题还是页面问题。

### 阶段 8：存储迁 IndexedDB `storage.js`

**状态：** 先不做。  
**原因：** 这是换存储，不是为了方便改代码。导入规则没锁住就换介质，出问题分不清是分流错了还是写入错了。也不要和拆 `theme.js` 放在同一步。

**为什么必须排在阶段 5、7 之后：**

- 先靠阶段 2 抽出 `createDefaultAppData` 和旧数组迁移，存储层只做 get / set，不再复制一份迁移逻辑。
- 先靠阶段 5 把导入分流锁住。导入走的是内存里的 `appData`，最后才 `save()`；换存储后导入规则不用改。
- 阶段 7 的冒烟现在可能读 `localStorage`。IndexedDB 放在它后面，那条改成「刷新后还在」即可。

**从 `main.js` 抽出：**

- `save()` / `saveThemeConfig()` 的写入
- `init()` / `loadThemeConfig()` 的读取
- 本机 `localStorage` → IndexedDB 的一次性搬迁

建议接口：

```js
export function loadPersistedState({ localStorage })
export function persistAppData(appData)
export function persistThemeConfig(themeConfig)
```

`loadPersistedState` 的读取顺序：

1. IndexedDB 已有书签数据，就用它。
2. 没有再读 `webManagerDataProMax` / `webManagerThemeConfig`。
3. 读到的旧档交给阶段 2 已抽出的结构迁移（数组 → `workspaces`），再写入 IndexedDB。

这是一次性拷贝，不是把旧 JSON 备份作废。工具栏、列数、滚动位置等小偏好可以暂时留在 `localStorage`。

**旧数据还能用：**

| 来源 | 迁完后 |
|---|---|
| JSON / ZIP 备份 | 能。导入仍解析同一份结构，只是最后写入 IndexedDB。`customIcon`、主题 `bgValue`（含 `data:image`）也还能吃。 |
| 本机 `localStorage` 旧档 | 能。启动时按上面的顺序搬一次。 |
| 大图备份 | 比现在更不容易因 `QuotaExceededError` 静默失败。 |

**注意事项：**

- `file://` 和本地服务器不是同一个源，两边的数据不会自动共用，这和现在的 `localStorage` 一样。
- IndexedDB 是异步的，`init()` 不能再同步 `getItem` 完立刻渲染。
- 不要改备份 JSON 字段，也不要顺手改导入分流。
- 迁成功后再决定是否删除 `localStorage` 旧键；第一轮建议先保留，确认刷新无误后再清。
- 双击打开 `index.html` 的方式不变。若某个浏览器在 `file://` 下禁用 IndexedDB，要有失败提示，并暂时回退读 `localStorage`，不要空白启动。

**测试文件：** `tests/storage.test.js`

必测：

- IndexedDB 已有数据时，不再读 `localStorage`
- 只有 `localStorage` 旧档时，能搬到 IndexedDB；数组格式先走结构迁移
- 主题配置 `webManagerThemeConfig` 同样能搬
- JSON / ZIP 备份导入后，写入的是 IndexedDB 而不是 `localStorage`
- 保存失败要能被调用方感知，不能静默丢数据

阶段 7 冒烟同步改一条：刷新后分类 / 网页还在，不要断言 `localStorage.getItem('webManagerDataProMax')`。

**完成标准：** 本机旧档刷新后还在；导出再导入树结构一致；大图保存不再因配额静默失败。

### 阶段 9：后期拆，低优先级测试

**状态：** 推荐先不拆。文件难读了再做。  
**原因：** 改这些功能时本来就要看 DOM 和 `index.html`；提前拆开只是多跳文件，测试也几乎帮不上。

以下可以拆文件，方便阅读，但不是测试重点，**不是「拆完才方便开发」的前提**：

| 模块 | 内容 | 测试策略 | 先不拆的原因 |
|---|---|---|---|
| `theme.js` | 日夜主题、背景、CSS 预设、旧主题迁移 | 只测旧配置迁到 `day/night`；不测滑条和裁图 | 滑条、预览、裁图绑在一起，拆了还是要来回看 |
| `toolbar.js` | 工具栏显隐、排序、列数 | 可不测，或只测配置合并 | `initToolbar()` 的合并逻辑很小，单独建文件不划算 |
| `icon.js` | 图标类型、批量改名 | 只测模板和过滤条件 | 模板已归阶段 3；过滤条件等真要改批量规则再抽 |
| `tools.js` | 死链、图标识别 | 保持手动；不要用真实网络做 CI | `checkLinks` 结果不稳定，不适合当回归 |

拖拽排序、Cropper、主题预览、`createNodeEl`、长按菜单、编辑弹窗继续留在 `main.js`，不写自动化。

`COMMON_SITES_DICTIONARY` / `fetchPageTitleFor` 也不要新开 `title.js`。字典可测的话并进现有 `api.js`；现在不是加紧项。

## 6. 推荐技术选型

| 用途 | 选择 | 原因 |
|---|---|---|
| 单元测试 | Vitest + jsdom | 原生支持 ES module，和当前页面一致 |
| 页面冒烟 | Playwright | 现有刷新断言先够用；阶段 5 后再补「主题文件不当书签」 |
| 运行页面 | 继续静态 HTML | 双击 / 本地服务器都能用 |
| 不要用 | Jest、Cypress、覆盖率门禁、打包器 | 对这个个人工具过重 |

测试夹具放 `tests/fixtures/`，至少准备：

- `legacy-array.json`：旧版树数组（已有）
- `full-backup.json`：带多个主页和分组的完整备份
- `theme-config.json`：主题文件，用于确认不会当书签导入
- `icon-settings.json`：图标配置
- `text-export.txt`：`generateTextExport` 的标准样本

阶段 5 时把缺的备份 / 主题 / 图标夹具补齐。

## 7. 每阶段手工验收

自动化通过后，至少手动点这几下：

1. 打开 `index.html`
2. 新建分类、新建网页、刷新，数据还在
3. 搜索刚建的网页，能跳转
4. 导出当前主页 JSON，清空后再导回来
5. 控制台没有新增报错

任何一阶段如果手工验收失败，先回退该阶段的拆分，不要继续拆下一块。

## 8. 明确不在本计划内

这些事重要，但不走「拆文件 + 单测」这条线，避免计划被带跑：

- 修复 `deleteCategoryModal` / `batchDeleteModal` 破损 HTML
- 自定义代码默认只允许 CSS
- 把 117 处 HTML `onclick` 全改成 `addEventListener`
- 把树渲染、拖拽、编辑弹窗拆成独立 UI 文件
- 把 `main.js` 拆到几十 KB

`eval(name)` 换成 handler 表、以及搜索 / 树选择的名称转义，见 `阶段1之后-阶段2之前.md`。那两步已完成，不要再和抽 `workspace.js` 放进同一次提交。

IndexedDB 已纳入阶段 8，不要提前做，也不要和阶段 2 的结构迁移混在一次改动里。

## 9. 建议的开工顺序

阶段 0–2 和 handler 表已经完成。接下来按这个顺序，不要按旧编号 3 → 4 → 5 → 6：

1. **阶段 5：** `io.js` + `tests/io.test.js`（导入分流 + 导出命名）
2. **阶段 1 补遗：** `tree.js` 补插入 /「未分类」，立刻补测试
3. **阶段 3：** `text-io.js`
4. **阶段 6：** `duplicates.js`
5. **有空再做阶段 4：** `search.js`
6. **阶段 5 之后可选：** 冒烟补「导入主题不会覆盖书签」

做到第 4 步，对「新增功能、修 bug 更方便」有用的部分就齐了。不要跳到主题、拖拽，也不要在阶段 5 之前改 IndexedDB。

## 10. 推荐先不拆

| 项 | 原因 |
|---|---|
| `theme.js` / 滑条 / 背景 / 裁图 | 改时必须对着页面；拆文件帮不上测试 |
| `toolbar.js` | 配置合并只有一小段，现在建文件是空转 |
| `icon.js` / `getIconHtml` / Cropper | 渲染和裁图不是 JSON 规则 |
| `tools.js` / `checkLinks` / 图标识别报告窗 | 依赖真实网络，结果不稳 |
| `storage.js` / IndexedDB | 换介质前必须先锁导入；现在做会把问题搅在一起 |
| `createNodeEl`、长按菜单、编辑弹窗、分类拖拽 | 计划明确不要为测试硬拆 UI |
| 整页 5 条 Playwright | 现有刷新冒烟够用；导入单测比再写 E2E 值钱 |
| 新文件 `save.js` / `export.js` / `title.js` / `io-ui.js` | 规则分别属于 `tree.js`、`io.js`、`api.js`；再拆只是换地方堆 |

停在「数据规则有模块和测试，`main.js` 只剩状态、事件、渲染、弹窗」即可。不必追求把入口文件拆小。
